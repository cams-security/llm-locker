[project]
name = "llm-locker-mcp"
version = "1.0.0"
description = "MCP tools for the llm-locker memory API"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=2.0.0",
    "httpx>=0.28.1",
    "cryptography",
    "python-dotenv",
]
